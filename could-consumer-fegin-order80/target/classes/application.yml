server:
  port: 80

eureka:
  client:
    register-with-eureka: false
    service-url:
      defaultZone: http://eureka7001.com:7001/eureka/ # http://eureka7002.com:7002/eureka/,
ribbon:
  ReadTimeout: 5000 #设置最大时间，默认为1秒
  ConnectTimeout: 5000

feign:
  hystrix:
    enabled: true